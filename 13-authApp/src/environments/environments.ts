
export const environment = {
    baseUrl: 'http://localhost:3000'  //baseUrl es el endpoint adonde quiero llegar

    //baseUrl: 'https://nest-auth-backend.up.railway.app' que es donde se desplega a producción si uso railway
}